[package]
name = "foobar"
version = "0.1.0"
authors = ["[your_name] <[your_email]>"]
edition = "2021"

[dependencies]
polkavm-derive = {  version = "0.18.0", default-features = false }
#ink = { path = "../../../../crates/ink", default-features = false, features = ["no-panic-handler"] }
#foo = { path = "../../../../foo", default-features = false }
#codec = { package = "parity-scale-codec", version =  "3.6.12", default-features = false }

#[dev-dependencies]
#ink_e2e = { path = "../../../../crates/e2e", features = ["sandbox"] }
spin = "0.9.8"
talc = "4.4.2"

[[bin]]
name = "foobar"
path = "lib.rs"

[features]
default = ["std"]
std = [
    #"ink/std",
]
ink-as-dependency = []
e2e-tests = []
